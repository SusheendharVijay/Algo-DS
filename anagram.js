function anagram(text1, text2) {
  let letters = {};
  
  for (let char of text1) {
    letters[char] = (letters[char] || 0) + 1;
  }

   for (let key in letters){

  if(!(keey in frequencyCounter2)

}
